/vendor/
/.phpstan.cache/
/.phpunit.cache/
/*.csv
*.progress.json
.env
.env.*
.idea/
.vscode/
.DS_Store

# Separate local project (Python/Node, own xray proxy credentials, node_modules,
# xray.exe binary) used at runtime as an optional bypass proxy - never commit it
# here, it doesn't belong in this Composer package's repo.
/benzpars-main/
