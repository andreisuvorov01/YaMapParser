fastapi>=0.111.0
uvicorn>=0.30.0
httpx[http2,socks]>=0.27.0
# Required by httpx SOCKS proxy support on Linux bench/fetch fallback.
socksio>=1.0.0
aiohttp>=3.9.0
pydantic>=2.0.0
# Опционально — реальная подмена TLS fingerprint (JA3/JA4):
# curl_cffi>=0.7.0
# Опционально — stealth-браузер:
# playwright>=1.44.0
# Импорт конфигов из share-ссылок (vless://, vmess://, trojan://, ss://):
#   используется встроенный httpx, дополнительные зависимости не требуются
